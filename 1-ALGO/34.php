<?php
/*Ecrivez un algorithme calculant la somme des valeurs d'un tableau (on suppose que le tableau a été préalablement saisi).
*/

//On déclare le tableau et la somme
$tab = array () ;
$somme = 0 ;
//$a = nombre de valeurs dans le tableau

//on additionne chaque valeur du tableau grâce à la boucle pour .
for ($i=0; $i < $a; $i++) { 
    $somme = $somme + $tab[$i];
}
?>